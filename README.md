# Create MCP Agent 🤖

A lightweight CLI tool to instantly scaffold a pre-configured MCP (Model Context Protocol) Agent project with custom rules and structure.

## Quick Start

You don't need to clone this repository. Just run the following command in your terminal where you want to create your agent:

```bash
npx github:Legurr/create-mcp-agent
